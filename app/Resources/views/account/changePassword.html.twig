{% extends 'layout/base.html.twig' %}
{% block body %}

    <div class="container" style="margin-top: 90px">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <p>
                    Vous souhaiter changer de mot de passe, aucun problème remplissez juste les champs suivants !
                </p>
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h3 class="panel-title"><strong>Changement de mot de passe</strong></h3>
                    </div>
                    <div class="panel-body">

                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                                <div class="form-group">
                                    <button class="form-control btn btn-primary col-md-4">Je change mon mot de passe !</button>
                                </div>
                        {{ form_end(form) }}

                        <div class="space"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}